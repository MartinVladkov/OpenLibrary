<UserControl x:Class="OpenLibrary.Views.SearchLibraryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:OpenLibrary.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="auto"/>
            </Grid.ColumnDefinitions>

            <TextBox Grid.Column="0" Margin="5 10 0 5" Text="{Binding SearchTerm, UpdateSourceTrigger=PropertyChanged}" FontSize="20"/>
            <Button Grid.Column="1" Margin="10 10 5 5" Content="Search" Command="{Binding SearchCommand}"/>
        </Grid>
        <Grid Grid.Row="1">
                <StackPanel Grid.Column="2" Orientation="Horizontal" Margin="20">
                <TextBlock Text="Search By:" Margin="0 0 25 0"/>
                <RadioButton Width="150" Content="Title" Name="cbIndia" IsChecked="{Binding Path=SearchByTitle, Mode=TwoWay}"></RadioButton>
                <RadioButton Width="150" Content="Author" Name="cbUSA" IsChecked="{Binding Path=SearchByAuthor, Mode=TwoWay}"></RadioButton>
            </StackPanel>
        </Grid>
        
        <!--<ListView Grid.Row="2" Margin="0 55 0 0" ItemsSource="{Binding Books}">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Author">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Author}"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>

                    <GridViewColumn Header="Title">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Title}"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                </GridView>
            </ListView.View>
        </ListView>-->

        <DataGrid Grid.Row="2" Name="dgUsers" Margin="0 55 0 0" AutoGenerateColumns="False" ItemsSource="{Binding Books}" DataGrid.GridLinesVisibility="None" FontSize="15">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Author" Binding="{Binding Author}" />
                <DataGridTextColumn Header="Title" Binding="{Binding Title}" />
            </DataGrid.Columns>
            <DataGrid.RowDetailsTemplate>
                <DataTemplate>
                    <DockPanel Background="GhostWhite">
                        <Image DockPanel.Dock="Left" Source="{Binding Cover}" Height="500" Margin="10" />
                        <Grid Margin="0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>
                            
                            <TextBlock Text="ISBN: " FontWeight="Bold" Margin="5 5 5 5"/>
                            <TextBlock Text="{Binding Isbn}" Grid.Column="1" Margin="5 5 5 5"/>
                            <TextBlock Text="Title: " FontWeight="Bold" Grid.Row="1" Margin="5 5 5 5"/>
                            <TextBlock Text="{Binding Title}" Grid.Column="1" Grid.Row="1" Margin="5 5 5 5"/>
                            <TextBlock Text="Author: " FontWeight="Bold" Grid.Row="2" Margin="5 5 5 5"/>
                            <TextBlock Text="{Binding Author}" Grid.Column="1" Grid.Row="2" Margin="5 5 5 5"/>
                            <TextBlock Text="Publish Year: " FontWeight="Bold" Grid.Row="3" Margin="5 5 5 5"/>
                            <TextBlock Text="{Binding PublishYear}" Grid.Column="1" Grid.Row="3" Margin="5 5 5 5"/>
                            <TextBlock Text="Number of pages: " FontWeight="Bold" Grid.Row="4" Margin="5 5 5 5"/>
                            <TextBlock Text="{Binding NumberOfPages}" Grid.Column="1" Grid.Row="4" Margin="5 5 5 5"/>

                        </Grid>
                    </DockPanel>
                </DataTemplate>
            </DataGrid.RowDetailsTemplate>
        </DataGrid>
        
        
    </Grid>
</UserControl>
